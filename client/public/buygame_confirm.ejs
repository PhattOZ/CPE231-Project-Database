<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="compiled.css">
    <title>Confirm transaction</title>
</head>
<style>

    .footer {
       width: 100%;
    }
</style>
<body class="font-kanit font-light
bg-gradient-to-tr from-indigo-900 via-cyan-blackest to-cyan-blackest text-white min-h-screen">

    <!--import nav bar-->
    <%- include("./nav") %>

    <div class="flex justify-center">
        <div class="space-y-6">
            <div class="pt-10 text-center text-2xl font-semibold">Review Order</div>
            <div>username : <%= username %></div>
            <div class="border-t-2">Game</div>
            <div> <%= gamedata.name %> : <%= gamedata.price %> THB </div>
            <div class="border-t-2">DLC</div>
            <% for(d of dlcdata) { %>
            <div>- <%= d.name %> : <%= d.price %> </div>
            <% } %>
            <div class="border-t-2 pt-2">Total :   <span class="text-xl"><%= totalprice %> THB</span> </div>
            <form
                action="/buygame_success?username=<%= username %>&gamename=<%= gamedata.name %>&dlcname=<%= dlcstring %>&total=<%= totalprice %> "
                method="POST">
                <button type="submit" class="hover:text-blue-300 pl-24">CONFIRM</button>
            </form>
        </div>
    </div>
    <div class="p-20"></div>


</body>
<footer>
    <div class="footer pt-10">
        <%- include ("./footer") %> 
        </div>
</footer>
</html>